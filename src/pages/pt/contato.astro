---
import PageLayout from '../../layouts/PageLayout.astro';
---

<PageLayout
	title="Contato"
	description="Entre em contato para propostas, dúvidas ou feedback."
>
	<p>
		Tem alguma pergunta, proposta ou apenas quer dizer um oi? Use o formulário abaixo. Fico feliz em ouvir o que você tem a dizer.
	</p>

	<form action="https://formspree.io/f/xzzvaylo" method="POST">
		<div class="form-group">
			<label for="name">Seu Nome</label>
			<input type="text" id="name" name="name" required />
		</div>

		<div class="form-group">
			<label for="email">Seu E-mail</label>
			<input type="email" id="email" name="email" required />
		</div>

		<div class="form-group">
			<label for="subject">Assunto</label>
			<select id="subject" name="subject" required>
				<option value="" disabled selected>Selecione um motivo...</option>
				<option value="Proposta de Projeto">Proposta de Projeto</option>
				<option value="Feedback sobre o Blog">Feedback sobre o Blog</option>
				<option value="Dúvida Técnica">Dúvida Técnica</option>
				<option value="Outro">Outro</option>
			</select>
		</div>

		<div class="form-group">
			<label for="message">Sua Mensagem</label>
			<textarea id="message" name="message" rows="6" required></textarea>
		</div>

		<div class="form-group-checkbox">
			<input type="checkbox" id="consent" name="consent" required />
			<label for="consent">
				Eu concordo em compartilhar minhas informações e entendo que elas serão usadas para me contatar em resposta à minha mensagem.
			</label>
		</div>

		<button type="submit">Enviar Mensagem</button>
	</form>
</PageLayout>

<style>
	form {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
		margin-top: 2rem;
	}
	.form-group {
		display: flex;
		flex-direction: column;
	}
	.form-group label {
		font-weight: 600;
		margin-bottom: 0.5rem;
		color: rgb(var(--black));
	}
	.form-group input,
	.form-group select,
	.form-group textarea {
		padding: 0.75rem;
		border: 1px solid rgb(var(--gray-light));
		border-radius: 8px;
		font-size: 1rem;
		background-color: #111111; /* Cinza quase preto */
		color: #ffffff; /* Texto branco */
	}
	.form-group select option {
		background-color: #111111;
		color: #ffffff;
	}
	.form-group input:focus,
	.form-group select:focus,
	.form-group textarea:focus {
		outline: none;
		border-color: var(--green);
		box-shadow: 0 0 0 2px var(--green-light);
	}
	.form-group-checkbox {
		display: flex;
		align-items: center;
		gap: 0.75rem;
	}
	.form-group-checkbox input {
		width: 1.25rem;
		height: 1.25rem;
	}
	.form-group-checkbox label {
		font-size: 0.9rem;
		color: rgb(var(--gray-dark));
	}
	button {
		padding: 1rem;
		border: none;
		border-radius: 8px;
		background-color: var(--green);
		color: #000;
		font-size: 1.1rem;
		font-weight: 700;
		cursor: pointer;
		transition: background-color 0.2s ease;
	}
	button:hover {
		background-color: var(--green-bright);
	}
</style>
